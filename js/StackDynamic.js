// JavaScript arrays are always dynamic, but we simulate user input
let size = 5; // Simulating runtime input
let arr = new Array(size);
for (let i = 0; i < size; i++) arr[i] = i + 1;
console.log(arr);
